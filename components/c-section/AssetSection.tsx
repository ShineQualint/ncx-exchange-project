import React from "react";

type Props = {};

const AssetSection = (props: Props) => {
  return (
    <section className="assetListContainer">
      <h1>Asset list</h1>
    </section>
  );
};

export default AssetSection;
